[package]
name = "golomb_vanguard"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
rayon = "1.10"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
opt-level = 3

[features]
default = []
stats = []
